*** SOTTSC ***
STORE '0' TO CHOICE
@ 0,0 CLEAR
@ 1,70 SAY "SERENO"
@ 5,23 SAY "ELENCO PRODOTTI SOTTOSCORTA E GESTIONE TOTALI DITTA"
@ 6,22 SAY REPLICATE("ð",29)
@ 10,22 SAY "0)  RITORNO AL MENU' PRINCIPALE"
@ 12,22 SAY "1)  ELENCO PR.SOTTOSCORTA"
@ 14,22 SAY "2)  TOTALI ACQUISTI E CONSUMI PER DITTA"
@ 15,22 SAY "3)  CORREZZIONE MAGAZZINO ANNO PRECEDENTE"
@ 18,23 SAY "SCELTA"
@ 1,0 SAY DATE()
@ 18,31 GET CHOICE
READ
DO CASE
 CASE CHOICE= '0'
  RETURN
 CASE CHOICE='1'
*           USE ARKIVIO INDEX PRODO
*       REPORT FORM SS1 FOR (GIADIN < 1 .AND. PROGSCA#0);
       .OR. (SCMIN >0 .AND. SCMIN < GIADIN) NOEJECT TO PRINT
CASE CHOICE='2'
        DO ARKTOT
CASE CHOICE='3'
	DO UPDATOLD
ENDCASE
flush
*** FINE SOTTSC ***
